# LLM integration tests

This folder is reserved for tests that exercise the real LLM/provider integration (e.g. `graph.py` and `openai_interface.py`).

These tests MAY require network access and credentials.

By default, the normal integration test suite should run with Mock LLM only.
