# Integration tests

This folder is for higher-level tests that exercise multiple modules together (e.g., running the CLI with `--mock-llm`, multi-file flows, etc.).

Keep unit tests in `test/unittests/`.
